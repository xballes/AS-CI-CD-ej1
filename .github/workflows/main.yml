name: Mi workflow

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Bajar codigo
        uses: actions/checkout@v4

      - name: Probar codigo
        run: python3 test.py

  build_push:
    runs-on: ubuntu-latest
    needs: [test]
    steps:
      - name: Bajar codigo
        uses: actions/checkout@v4

      
      
  

